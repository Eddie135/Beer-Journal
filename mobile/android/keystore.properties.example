# Copy to keystore.properties outside Git and replace every placeholder.
# The file is ignored by .gitignore. Never commit passwords or a real keystore.
storeFile=C:/Users/EDY/Documents/BeerJournalBackups/beer-journal-release.keystore
storePassword=CHANGE_ME
keyAlias=beer-journal
keyPassword=CHANGE_ME
